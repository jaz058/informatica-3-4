#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    srand(time(NULL));
    int dice1 = (rand() % 6) + 1; // Generates a number between 1 and 6
    printf("You rolled a: %d\n", dice1);
    return 0;
}

    srand(time(NULL));
    int dice2 = (rand() % 2) + 1; // Generates a number between 1 and 6
    printf("You rolled a: %d\n", dice2);
    return 0;
    
    
    srand(time(NULL));
    int dice3 = (rand() % 6) + 1; // Generates a number between 1 and 6
    printf("You rolled a: %d\n", dice3);
    return 0;

    int main() {
    srand(time(NULL));
    int dice4 = (rand() % 6) + 1; // Generates a number between 1 and 6
    printf("You rolled a: %d\n", dice4);
    return 0;